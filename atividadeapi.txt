CREATE TABLE LIVROS (
	ID_LIVROS integer GENERATED BY DEFAULT AS IDENTITY,
	TITULO varchar(255) NOT NULL,
	AUTOR varchar(255) NOT NULL,
	ANO_PUBLICACAO integer
);

ALTER TABLE LIVROS ADD PRIMARY KEY (ID_LIVROS)

INSERT INTO livros(titulo, autor, ANO_PUBLICACAO)
values('coisa', 'Jorge', 2008)

INSERT INTO livros(titulo, autor, ANO_PUBLICACAO)
values('negocio', 'Thomas', 2009)

INSERT INTO livros(titulo, autor, ANO_PUBLICACAO)
values('pegadinha', 'Felipe', 2010) 


CREATE TABLE USUARIOS (
	ID_USUARIOS integer GENERATED BY DEFAULT AS IDENTITY,
	NOME varchar(255) NOT NULL,
	EMAIL varchar(255) NOT NULL,
	SENHA varchar(255) NOT NULL
);

INSERT INTO usuarios(nome, email, senha)
values('paulinho', 'paulinho@gmail.com', 'Paulo@2000') 

INSERT INTO usuarios(nome, email, senha)
values('marcelo', 'marcelo@gmail.com', 'Marcelo@2000') 

INSERT INTO usuarios(nome, email, senha)
values('kaio', 'kaio@gmail.com', 'Kaio@2000') 

DROP TABLE USUARIOS